"""Split-out VM integration scenarios (placeholder)."""

# Existing scenarios remain in ``tests/test_boot_image_vm.py`` until migration
# is complete. This file will eventually hold the split tests so they can be
# maintained independently of fixture definitions.

"""Helpers and scenarios for boot-image VM integration tests."""

# The new modules under ``tests.vm`` are being built incrementally from
# ``tests/test_boot_image_vm.py``. Keep imports lightweight to avoid adding
# dependencies while code is migrated.
